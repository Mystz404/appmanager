# AppManager 自更新 - 快速开始指南

## ⚡ 5分钟快速启用

### 第一步：编译 AppManager

```bash
cd d:\WorkSpace\QtProjects\AppManager\build\Desktop_Qt_5_15_2_MinGW_32_bit-Release
mingw32-make -j4
```

验证编译成功：
```bash
Get-Item release\AppManager.exe | Select-Object -ExpandProperty VersionInfo
```

### 第二步：生成安装程序

```bash
cd d:\WorkSpace\QtProjects\AppManager\installer
.\build_installer.ps1 -Version 1.0.0
```

验证输出：
```bash
Get-Item ..\installer_output\AppManagerSetup.exe
```

### 第三步：准备服务器

#### 选项 A：本地文件服务器（推荐测试用）

```bash
cd d:\WorkSpace\QtProjects\AppManager\示例文件

# PowerShell 5.1+ 自带 HTTP 服务器
$listener = New-Object System.Net.HttpListener
$listener.Prefixes.Add("http://+:8080/")
$listener.Start()

Write-Host "服务器已启动: http://localhost:8080"
Write-Host "按 Ctrl+C 停止服务器"

while ($listener.IsListening) {
    $ctx = $listener.GetContext()
    $req = $ctx.Request
    $res = $ctx.Response
    
    $path = $req.Url.LocalPath -replace '^/', ''
    $file = Join-Path (Get-Location) $path
    
    if (Test-Path $file) {
        $res.ContentType = 'application/octet-stream'
        $fs = [System.IO.File]::OpenRead($file)
        $fs.CopyTo($res.OutputStream)
        $fs.Close()
        $res.StatusCode = 200
    } else {
        $res.StatusCode = 404
    }
    
    $res.Close()
}
```

#### 选项 B：IIS 服务器

1. 在 IIS 中创建网站或虚拟目录
2. 复制 `示例文件/` 目录内容到网站根目录
3. 配置目录权限允许读取

#### 选项 C：Nginx

```bash
# 配置示例
server {
    listen 8080;
    root d:\WorkSpace\QtProjects\AppManager\示例文件;
    location / {
        autoindex on;
    }
}
```

### 第四步：配置客户端

编辑 `apps.json`：

```json
{
  "serverBaseUrl": "http://your-server-ip:8080",
  "appsRoot": ".",
  "apps": [...]
}
```

更新 `示例文件/updates/AppManager.json`：

```json
{
  "latestVersion": "2.0.0",
  "downloadUrl": "http://your-server-ip:8080/releases/AppManagerSetup_2.0.0.exe"
}
```

### 第五步：测试

1. 启动 AppManager
2. 菜单 → 帮助 → 检查 AppManager 更新
3. 验证流程：
   - ✅ 检查版本（应显示当前版本）
   - ✅ 发现新版本（如已配置）
   - ✅ 下载进度（显示百分比）
   - ✅ 启动安装程序（应自动关闭应用）
   - ✅ 安装完成（应自动重启应用）

## 🎯 常见场景

### 场景 1：本地测试（无真实服务器）

测试环境：所有文件在同一台机器

1. 修改 `apps.json`：
   ```json
   {
     "serverBaseUrl": "http://127.0.0.1:8080",
     ...
   }
   ```

2. 启动本地文件服务器：
   ```bash
   cd d:\WorkSpace\QtProjects\AppManager\示例文件
   # 使用上述 PowerShell 服务器脚本
   ```

3. 关键点：
   - ✅ /updates/AppManager.json 必须存在
   - ✅ /releases/AppManagerSetup_*.exe 必须存在
   - ✅ downloadUrl 必须可访问

### 场景 2：局域网部署

客户端和服务器在不同机器

1. 在服务器上配置：
   ```bash
   $serverIp = "192.168.1.100"
   $serverPath = "\\$serverIp\share\AppManager"
   
   # 跨机器复制文件
   Copy-Item "installer_output\AppManagerSetup.exe" "$serverPath\releases\"
   Copy-Item "示例文件\updates\AppManager.json" "$serverPath\updates\"
   ```

2. 在客户端上配置 `apps.json`：
   ```json
   {
     "serverBaseUrl": "http://192.168.1.100:8080",
     ...
   }
   ```

### 场景 3：互联网部署

客户端在互联网访问更新

1. 使用云服务器或 CDN
2. 配置 HTTPS：
   ```json
   {
     "serverBaseUrl": "https://updates.example.com",
     ...
   }
   ```

3. 部署脚本（使用 SCP）：
   ```bash
   $remoteServer = "user@example.com:/var/www/appmanager"
   scp installer_output\AppManagerSetup.exe "$remoteServer/releases/"
   scp 示例文件\updates\AppManager.json "$remoteServer/updates/"
   ```

## 🔍 故障排查

### 问题：检查更新后无响应

**步骤 1**：验证网络连接
```bash
# 测试网络访问
Test-Connection your-server-ip
```

**步骤 2**：验证 URL 可访问
```bash
# PowerShell
Invoke-WebRequest "http://your-server-ip:8080/updates/AppManager.json"

# 或直接在浏览器中访问
```

**步骤 3**：检查 JSON 语法
```bash
# PowerShell
$json = Get-Content "示例文件\updates\AppManager.json" | ConvertFrom-Json
$json
```

### 问题：下载失败

**检视清单**：

- [ ] 服务器上的安装程序文件存在
- [ ] 文件名与 JSON 中的 downloadUrl 一致
- [ ] 文件权限允许读取（Windows: Everyone=Read）
- [ ] 磁盘空间充足 (%TEMP% 目录)

### 问题：安装程序无法启动

**原因分析**：

```
下载的 EXE 损坏 → 手动测试下载的文件
┌─────────────┬─────────────────┐
│ 正常        │ 异常
v             v
文件完整      文件不完整或下载中断
│             │
检查权限      检查网络、磁盘空间
```

### 问题：安装后应用未重启

**原因**：这是正常行为

**解决方案**：
1. 手动启动新版本应用
2. 或等待安装程序完成后重启系统

## 📋 检查清单

部署前验证：

```
┌─ 客户端配置
│  ├─ [ ] apps.json 包含有效的 serverBaseUrl
│  ├─ [ ] serverBaseUrl 不为空
│  └─ [ ] serverBaseUrl 格式正确 (http://...)
│
├─ 服务器文件
│  ├─ [ ] /updates/AppManager.json 存在
│  ├─ [ ] /updates/AppManager.json 格式有效
│  ├─ [ ] /releases/AppManagerSetup_*.exe 存在
│  └─ [ ] downloadUrl 指向正确的文件路径
│
├─ 网络配置
│  ├─ [ ] 客户端可以 ping 服务器
│  ├─ [ ] 服务器端口开放 (8080 或配置的端口)
│  ├─ [ ] 防火墙允许 HTTP/HTTPS
│  └─ [ ] 代理配置（如需要）
│
├─ 文件权限
│  ├─ [ ] 服务器文件权限正确（可读）
│  ├─ [ ] 客户端 %TEMP% 目录可写
│  └─ [ ] AppManager 安装目录可写
│
└─ 功能验证
   ├─ [ ] 能访问 /updates/AppManager.json
   ├─ [ ] JSON 内容格式正确
   ├─ [ ] 能下载 AppManagerSetup_*.exe
   ├─ [ ] 能启动安装程序
   └─ [ ] 安装完成后应用自动重启
```

## 🚀 自动化部署

### PowerShell 一键部署脚本

```bash
# 1. 编译、生成安装程序、部署到服务器（三合一）
cd d:\WorkSpace\QtProjects\AppManager\build\Desktop_Qt_5_15_2_MinGW_32_bit-Release

# 2. 清空并重新编译
mingw32-make -j4 clean
mingw32-make -j4

# 3. 生成新版本安装程序
cd ..\installer
.\build_installer.ps1 -Version "2.0.0"

# 4. 部署到服务器
cd ..\示例文件
.\deploy_appmanager_to_server.ps1 -ServerIp 192.168.1.100 -Version "2.0.0"
```

### 定时自动检查更新（高级）

在客户端应用启动时自动检查（需要修改代码）：

```cpp
// 在 MainWindow 构造函数中添加
MainWindow::MainWindow(QWidget *parent) 
    : QMainWindow(parent), ui(new Ui::MainWindow)
{
    // ... 初始化代码
    
    // 应用启动 3 秒后自动检查更新
    QTimer::singleShot(3000, this, [this]() {
        if (m_serverConnected) {
            // 静默检查（不显示对话框）
            OnlineAppInfo info = m_service.checkAppManagerUpdate();
            if (info.requestSuccess && 
                compareVersions(m_service.appManagerVersion(), 
                               info.latestVersion) < 0) {
                // 显示软提示或通知栏
                appendLog(QStringLiteral("[AppManager] 发现新版本，点击菜单检查更新"));
            }
        }
    });
}
```

## 📊 版本号约定

推荐使用语义化版本（Semantic Versioning）：

```
版本号格式: MAJOR.MINOR.PATCH.BUILD
示例: 2.0.1.0

WHERE:
- MAJOR (2): 重大版本号，功能重构/架构变更
- MINOR (0): 次版本号，新增功能
- PATCH (1): 补丁版本号，bug 修复
- BUILD (0): 编译号，CI/CD 自动增加
```

在 Qt 项目中设置：

```pro
# AppManager.pro
VERSION = 2.0.1.0

QMAKE_TARGET_COPYRIGHT = Copyright 2024-2025
QMAKE_TARGET_PRODUCT = "AppManager"
QMAKE_TARGET_DESCRIPTION = "Application Manager and Updater"
```

## 🎓 更多学习资源

- 📖 [AppManager自更新实现说明.md](AppManager自更新实现说明.md) - 详细文档
- 📋 [自更新功能实现总结.md](自更新功能实现总结.md) - 技术详情
- 📂 [示例文件/](示例文件/) - 配置文件示例
- 🔨 [installer/build_installer.ps1](installer/build_installer.ps1) - 打包脚本
- 🚀 [示例文件/deploy_appmanager_to_server.ps1](示例文件/deploy_appmanager_to_server.ps1) - 部署脚本

## 💬 常见问题

**Q: 能否跳过自更新检查？**  
A: 可以，配置 `apps.json` 时不设置或清空 `serverBaseUrl` 即可禁用自更新功能。

**Q: 支持 HTTP 和 HTTPS 混合吗？**  
A: 支持。配置 `serverBaseUrl` 时直接使用对应的协议即可。

**Q: 如何实现灰度更新（只更新部分用户）？**  
A: 可在服务器端根据客户端信息返回不同的版本——需要修改元数据接口支持查询参数（如 clientId、version、uuid 等）。

**Q: 能否在后台运行安装程序（不显示界面）？**  
A: 已实现。使用 Inno Setup 的 `/SILENT` 和 `/NORESTART` 参数。

**Q: 更新失败时如何回滚？**  
A: Inno Setup 会在安装前备份旧版本，用户可在"添加/删除程序"中选择"卸载并保留数据"，然后重新安装旧版本。

---

**更新日期**: 2024-03-11  
**作者**: AppManager Team  
**版本**: 1.0
