# AppManager 自更新 - 常见问题与解决方案

此文档列举在自更新功能使用中的常见问题和对应的解决方案。

---

## 📌 目录

1. [编译和打包阶段问题](#编译和打包阶段问题)
2. [客户端配置问题](#客户端配置问题)
3. [服务端配置问题](#服务端配置问题)
4. [运行时问题](#运行时问题)
5. [下载和安装问题](#下载和安装问题)
6. [网络和连接问题](#网络和连接问题)
7. [版本和升级问题](#版本和升级问题)
8. [安全和权限问题](#安全和权限问题)

---

## 编译和打包阶段问题

### Q1: PowerShell 显示"在此系统上禁止运行脚本"

**症状：**
```
build_installer.ps1 : 在此系统上禁止运行脚本。
```

**原因：** Windows PowerShell 执行策略限制了脚本执行

**解决方案：**

**方案 A：临时解除（当前会话）** ✅ 推荐（最安全）
```powershell
# 打开 PowerShell，执行一次
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# 然后运行脚本
.\build_installer.ps1 -Version 1.0.0
```

**方案 B：永久解除（当前用户）** 
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

**方案 C：绕过 (仅此脚本)**
```powershell
powershell -ExecutionPolicy Bypass -File .\build_installer.ps1 -Version 1.0.0
```

**方案 D：解锁文件**
```powershell
Unblock-File .\build_installer.ps1
.\build_installer.ps1 -Version 1.0.0
```

---

### Q2: 找不到 Inno Setup

**症状：**
```
Inno Setup 编译器不存在
```

**原因：** 未安装 Inno Setup 或安装路径非标准

**解决方案：**

1. **检查安装**
   ```powershell
   Get-Command iscc  # 检查是否可用
   ```

2. **重新安装**
   - 下载：[www.jrsoftware.org/isinfo.php](https://www.jrsoftware.org/isinfo.php)
   - 选择"完整安装"
   - 安装到默认位置 `C:\Program Files (x86)\Inno Setup 6\`

3. **手动指定路径**
   编辑 `build_installer.ps1`：
   ```powershell
   $innoSetupPath = "C:\Program Files (x86)\Inno Setup 6\iscc.exe"
   if (-not (Test-Path $innoSetupPath)) {
       Write-Host "Inno Setup 路径错误或不存在"
       exit 1
   }
   ```

---

### Q3: windeployqt 找不到必要的 DLL

**症状：**
```
缺少 msvcr120.dll, msvcp120.dll 等文件
```

**原因：** Qt 部署不完整或运行时库缺失

**解决方案：**

1. **运行 windeployqt**
   ```powershell
   # 手动运行以查看详细输出
   cd build\Desktop_Qt_5_15_2_MinGW_32_bit-Release\release
   
   $qtPath = "C:\Qt\5.15.2\mingw81_32"
   & "$qtPath\bin\windeployqt.exe" AppManager.exe --release
   ```

2. **检查 Qt 安装**
   - 确保安装了对应版本：Qt 5.15.2 MinGW 32-bit
   - 检查安装路径：`C:\Qt\5.15.2\mingw81_32\`

3. **手动复制 DLL**
   ```powershell
   # 从 MinGW lib 目录复制
   copy "C:\Qt\5.15.2\mingw81_32\lib\libwinpthread-1.dll" `
        "build\...\release\"
   ```

---

### Q4: 版本号无法自动更新

**症状：**
生成的安装程序版本仍然是旧版本

**原因：** 脚本未正确更新版本号

**解决方案：**

1. **检查脚本参数**
   ```powershell
   .\build_installer.ps1 -Version 2.0.0
   # 必须显式指定版本号
   ```

2. **检查版本号替换**
   编辑 `installer/AppManager.iss`，确保包含版本占位符：
   ```ini
   #define AppVersion "{VERSION_PLACEHOLDER}"
   AppVersion={VERSION_PLACEHOLDER}
   ```

3. **手动编辑 AppManager.iss**
   ```ini
   #define AppVersion "2.0.0"
   AppVersion=2.0.0
   ```

---

## 客户端配置问题

### Q5: apps.json 中 serverBaseUrl 配置错误

**症状：**
```
检查更新失败
QNetwork 错误：无效的 URL
```

**原因：** serverBaseUrl 格式不正确

**解决方案：**

**正确格式：**
```json
{
    "serverBaseUrl": "http://192.168.1.100:6655",
    "apps": [...]
}
```

**常见错误：**
| 错误 | 正确 | 原因 |
|------|------|------|
| `192.168.1.100:6655` | `http://192.168.1.100:6655` | 缺少协议前缀 |
| `http://192.168.1.100:6655/` | `http://192.168.1.100:6655` | 末尾不要加斜杠 |
| `https://192.168.1.100:6655` | `http://192.168.1.100:6655` | 当前服务端不支持 HTTPS |
| `localhost:6655` | `http://127.0.0.1:6655` | 不支持 localhost |

**验证配置：**
```powershell
# PowerShell 验证 JSON 有效性
$config = Get-Content "AppManager\apps.json" | ConvertFrom-Json
Write-Host $config.serverBaseUrl  # 应显示 http://...
```

---

### Q6: appManagerVersion() 返回错误的版本号

**症状：**
```
菜单显示："当前版本：0.0.0"
```

**原因：** 版本号未正确编译或获取失败

**解决方案：**

1. **检查源代码中的版本号**
   编辑 `AppManager/appmanagerservice.cpp`：
   ```cpp
   QString AppManagerService::appManagerVersion() {
       return "1.0.0";  // 确保这个值正确
   }
   ```

2. **重新编译**
   ```bash
   cd build\Desktop_Qt_5_15_2_MinGW_32_bit-Release
   mingw32-make clean
   mingw32-make -j4
   ```

3. **检查 Qt 资源文件**
   如果使用了 VERSION 宏，确保 `.pro` 文件中配置了：
   ```pro
   VERSION = 1.0.0
   ```

---

### Q7: 菜单不显示"检查 AppManager 更新"

**症状：**
```
帮助菜单中没有"检查 AppManager 更新"选项
```

**原因：** 代码未正确编译或菜单初始化失败

**解决方案：**

1. **检查代码是否存在**
   编辑 `AppManager/mainwindow.cpp`：
   ```cpp
   // 在 MainWindow 构造函数中
   QAction *actionCheckAppManagerUpdate = helpMenu->addAction("检查 AppManager 更新");
   connect(actionCheckAppManagerUpdate, &QAction::triggered, 
           this, &MainWindow::onCheckAppManagerUpdate);
   ```

2. **重新编译**
   ```bash
   cd build\Desktop_Qt_5_15_2_MinGW_32_bit-Release
   mingw32-make clean
   qmake ..\AppManager.pro
   mingw32-make -j4
   ```

3. **检查 Qt 版本**
   某些旧版本 Qt 可能有菜单相关的 bug。确保使用 Qt 5.15.2。

---

## 服务端配置问题

### Q8: server_apps.json 中缺少 AppManager 条目

**症状：**
```
客户端检查更新时返回 404 Not Found
```

**原因：** 服务端配置文件未包含 AppManager

**解决方案：**

1. **编辑 server_apps.json**
   ```json
   {
       "apps": [
           {
               "appId": "AppManager",
               "appName": "AppManager",
               "latestVersion": "1.0.0",
               "packageFileName": "AppManagerSetup_1.0.0.exe",
               "sha256": "abc123...",
               "packageType": "exe",
               "subDir": "AppManager",
               "enabled": true
           },
           // 其他应用...
       ]
   }
   ```

2. **重启服务端**
   ```powershell
   # 关闭现有进程
   Stop-Process -Name UpdateServer -Force
   
   # 重启
   .\build_release\release\UpdateServer.exe
   ```

3. **验证**
   ```bash
   curl http://localhost:6655/updates/AppManager.json
   ```

---

### Q9: packages/AppManager/ 目录不存在

**症状：**
```
下载失败：404 Not Found
返回信息：File not found
```

**原因：** 升级包目录或文件缺失

**解决方案：**

1. **创建目录**
   ```powershell
   mkdir -Force UpdateServer\packages\AppManager
   ```

2. **复制安装程序**
   ```powershell
   copy "AppManager\installer_output\AppManagerSetup.exe" `
        "UpdateServer\packages\AppManager\AppManagerSetup_1.0.0.exe"
   ```

3. **验证文件**
   ```powershell
   dir UpdateServer\packages\AppManager\
   
   # 应显示
   # AppManagerSetup_1.0.0.exe (约 30-50 MB)
   ```

---

### Q10: updates/AppManager.json 未自动生成

**症状：**
```
检查更新成功，但返回空结果
```

**原因：** 服务端未正确生成元数据

**解决方案：**

1. **检查文件权限**
   ```powershell
   # 确保有写权限
   icacls "UpdateServer\updates" /grant:r "BUILTIN\Users":(OI)(CI)F
   ```

2. **手动检查生成**
   编辑 `UpdateServer/main.cpp`，确保启动时调用了生成方法：
   ```cpp
   appUpdateManager.generateAllStaticMeta();  // 应在启动时调用
   ```

3. **重启服务端**
   ```powershell
   .\build_release\release\UpdateServer.exe
   ```

4. **验证生成的文件**
   ```bash
   # 应该能看到 updates\AppManager.json
   curl http://localhost:6655/updates/AppManager.json
   
   # 返回内容如：
   # {
   #   "latestVersion": "1.0.0",
   #   "downloadUrl": "http://...",
   #   "sha256": "..."
   # }
   ```

---

### Q11: SHA256 哈希值计算错误

**症状：**
```
客户端验证：SHA256 不匹配
下载失败
```

**原因：** 服务端计算的 SHA256 与实际文件不符

**解决方案：**

1. **手动计算 SHA256**
   ```powershell
   $file = "UpdateServer\packages\AppManager\AppManagerSetup_1.0.0.exe"
   (Get-FileHash $file -Algorithm SHA256).Hash
   
   # 输出：abc123def456...
   ```

2. **更新 server_apps.json**
   ```json
   {
       "appId": "AppManager",
       "sha256": "abc123def456...",  // 更新为正确的值
       ...
   }
   ```

3. **重启服务端以重新生成元数据**
   ```powershell
   .\build_release\release\UpdateServer.exe
   ```

---

## 运行时问题

### Q12: 点击"检查 AppManager 更新"无反应

**症状：**
```
菜单响应缓慢或无反应
界面冻结
```

**原因：** 网络请求阻塞了主线程

**解决方案：**

这个问题通常表示代码正确（进行了异步处理），只需等待 3-5 秒。

```cpp
// 正确的实现（应该显示进度对话框）
void MainWindow::onCheckAppManagerUpdate() {
    QProgressDialog progressDialog("正在检查 AppManager 新版本...");
    // 异步网络请求
    // 不会冻结界面
}
```

如果界面确实冻结，检查 `AppManager/mainwindow.cpp`，确保没有阻塞调用。

---

### Q13: 进度对话框显示异常

**症状：**
```
对话框闪烁或显示不完整
无法看到进度信息
```

**原因：** Qt 事件循环问题或对话框配置错误

**解决方案：**

编辑 `AppManager/mainwindow.cpp`，确保进度对话框正确初始化：

```cpp
void MainWindow::onCheckAppManagerUpdate() {
    QProgressDialog progressDialog("正在检查 AppManager 新版本...", nullptr, 0, 0);
    progressDialog.setWindowModality(Qt::ApplicationModal);  // 模态对话框
    progressDialog.setCancelButton(nullptr);  // 无取消按钮
    progressDialog.show();
    
    qApp->processEvents();  // 强制重绘
    
    // 执行检查...
}
```

---

## 下载和安装问题

### Q14: 安装程序下载速度慢

**症状：**
```
下载进度缓慢（< 1 MB/s）
下载超时
```

**原因：** 网络质量差或服务端处理能力不足

**解决方案：**

1. **检查网络**
   ```bash
   ping 192.168.1.100
   # 应该 < 10 ms 延迟
   ```

2. **增加超时时间**
   编辑 `AppManager/appmanagerservice.cpp`：
   ```cpp
   QNetworkRequest request(downloadUrl);
   request.setHeader(QNetworkRequest::UserAgentHeader, "AppManager/1.0");
   // request.setTransferTimeout(30000);  // 增加到 30 秒
   ```

3. **优化服务端**
   - 确保网络带宽充足
   - 考虑使用内容分发网络（CDN）
   - 检查磁盘 I/O 是否成为瓶颈

---

### Q15: 下载文件损坏或不完整

**症状：**
```
安装程序启动时报错："EXE 文件损坏"
```

**原因：** 下载过程中文件不完整或被篡改

**解决方案：**

1. **启用 SHA256 验证**
   编辑 `AppManager/appmanagerservice.cpp`：
   ```cpp
   if (!appManagerService->verifySHA256(downloadedFile, expectedHash)) {
       qWarning() << "SHA256 验证失败";
       // 删除不完整的文件
       QFile::remove(downloadedFile);
       return false;
   }
   ```

2. **检查文件完整性**
   ```powershell
   # 手动验证
   $file = "C:\Users\User\AppData\Local\Temp\AppManagerSetup_1.0.0.exe"
   (Get-FileHash $file -Algorithm SHA256).Hash
   ```

3. **重新下载**
   通常自动重试即可。如果多次失败，检查服务端硬盘空间和文件权限。

---

### Q16: 安装程序启动失败

**症状：**
```
系统对话框："无法启动程序"
没有反应
```

**原因：** 下载的文件不是有效的 EXE 或文件权限问题

**解决方案：**

1. **检查文件有效性**
   ```powershell
   # 尝试手动运行
   & "C:\Users\User\AppData\Local\Temp\AppManagerSetup_1.0.0.exe" /help
   ```

2. **检查权限**
   ```powershell
   # 添加执行权限
   Unblock-File "C:\Users\User\AppData\Local\Temp\AppManagerSetup_1.0.0.exe"
   ```

3. **重新下载安装程序**
   删除现有的 exe，重新执行检查更新流程。

---

### Q17: 安装提示"无法写入目录"

**症状：**
```
Inno Setup 对话框：
Permission denied: C:\Program Files\AppManager
```

**原因：** 无足够权限修改 Program Files

**解决方案：**

1. **以管理员身份运行 AppManager**
   ```powershell
   # 创建快捷方式，配置为管理员运行
   Right-Click Start Menu → Advanced  → Run as Administrator
   ```

2. **更改安装位置（修改安装程序）**
   编辑 `AppManager/installer/AppManager.iss`：
   ```ini
   ; 改为用户目录
   DefaultDirName={userpf}\AppManager
   ; 或者
   DefaultDirName=C:\Apps\AppManager
   ```

3. **重新编译安装程序**
   ```powershell
   .\build_installer.ps1 -Version 1.0.0
   ```

---

## 网络和连接问题

### Q18: 网络延迟高导致超时

**症状：**
```
检查更新失败
QNetwork 错误：Connection Timeout
```

**原因：** 网络延迟 > 10 秒或连接断开

**解决方案：**

1. **检查网络连接**
   ```bash
   ping -t 192.168.1.100
   # 观察延迟和丢包率
   ```

2. **增加超时设置**
   编辑 `AppManager/appmanagerservice.cpp`：
   ```cpp
   QString AppManagerService::checkAppManagerUpdate() {
       QNetworkRequest request(updateCheckUrl);
       
       // 设置更多超时（毫秒）
       // 如果 setTransferTimeout 不可用，使用高级方法
       
       QTimer timeoutTimer;
       timeoutTimer.setInterval(60000);  // 60 秒
       connect(&timeoutTimer, &QTimer::timeout, [this]() {
           // 超时处理
       });
   }
   ```

3. **优化网络**
   - 使用有线连接替代 Wi-Fi
   - 避免高峰期（如中午或晚上）
   - 切换到更快的网络

---

### Q19: 防火墙阻止连接

**症状：**
```
检查更新失败
无法连接到 192.168.1.100:6655
```

**原因：** 防火墙规则阻止 6655 端口

**解决方案：**

**Windows 防火墙：**
```powershell
# 以管理员身份打开 PowerShell

# 添加入站规则
New-NetFirewallRule -DisplayName "Allow UpdateServer 6655" `
    -Direction Inbound -Protocol TCP -LocalPort 6655 -Action Allow

# 验证
Get-NetFirewallRule -DisplayName "Allow UpdateServer 6655"
```

**第三方防火墙（如 360、腾讯管家）：**
- 设置 → 防火墙 → 允许列表
- 添加 UpdateServer.exe 到白名单
- 允许 6655 端口的 TCP 入站连接

**企业网络：**
- 联系网络管理员
- 请求开放 6655 端口（或代理配置）

---

### Q20: 代理环境下无法连接

**症状：**
```
公司网络环境下检查更新失败
```

**原因：** 企业代理与直接 HTTP 冲突

**解决方案：**

编辑 `AppManager/appmanagerservice.cpp`，添加代理支持：

```cpp
#include <QNetworkProxy>

QString AppManagerService::checkAppManagerUpdate() {
    // 检测系统代理设置
    QNetworkProxyQuery npq(serverBaseUrl);
    QList<QNetworkProxy> proxies = QNetworkProxyFactory::systemProxyForQuery(npq);
    
    if (!proxies.isEmpty()) {
        QNetworkProxy proxy = proxies.first();
        if (proxy.type() != QNetworkProxy::NoProxy) {
            m_networkManager->setProxy(proxy);  // 使用系统代理
        }
    }
    
    // 继续发起请求...
}
```

---

## 版本和升级问题

### Q21: 版本比较逻辑不正确

**症状：**
```
服务端版本是 2.0.0，但客户端仍显示"已是最新版本"
```

**原因：** 版本号比较算法有误（如字符串比较而非语义比较）

**解决方案：**

编辑 `AppManager/appmanagerservice.cpp`，使用语义版本比较：

```cpp
bool AppManagerService::isNewerVersion(const QString &newVersion, 
                                       const QString &currentVersion) {
    // 分解版本号 "2.0.0" → {2, 0, 0}
    auto parts = [](const QString &ver) -> QVector<int> {
        QVector<int> result;
        for (const auto &part : ver.split('.')) {
            result.append(part.toInt());
        }
        return result;
    };
    
    QVector<int> newParts = parts(newVersion);
    QVector<int> curParts = parts(currentVersion);
    
    // 逐一比较版本号部分
    int maxSize = qMax(newParts.size(), curParts.size());
    for (int i = 0; i < maxSize; ++i) {
        int newVal = (i < newParts.size()) ? newParts[i] : 0;
        int curVal = (i < curParts.size()) ? curParts[i] : 0;
        
        if (newVal > curVal) return true;
        if (newVal < curVal) return false;
    }
    
    return false;  // 版本相同
}
```

---

### Q22: 跳过某个版本进行升级

**症状：**
```
从 1.0.0 直接升级到 2.0.0（跳过 1.5.0）
```

**解决方案：**

这通常是预期行为。如果需要支持中间版本强制升级，在 `server_apps.json` 中配置：

```json
{
    "appId": "AppManager",
    "currentSupportedVersions": [
        "1.5.0",
        "2.0.0"
    ],
    "latestVersion": "2.0.0",
    ...
}
```

修改客户端检查逻辑，在下载前验证当前版本在支持列表中。

---

### Q23: 版本回滚

**症状：**
```
新版本有 bug，需要降级到旧版本
```

**原因：** 版本管理不当

**解决方案：**

1. **保留旧版本**
   ```powershell
   # 不要删除旧的 EXE
   UpdateServer\packages\AppManager\
   ├── AppManagerSetup_1.0.0.exe
   ├── AppManagerSetup_1.5.0.exe
   └── AppManagerSetup_2.0.0.exe (有 bug)
   ```

2. **修改 server_apps.json**
   ```json
   {
       "appId": "AppManager",
       "latestVersion": "1.5.0",  // 回滚到 1.5.0
       "packageFileName": "AppManagerSetup_1.5.0.exe",
       ...
   }
   ```

3. **重启服务端**
   ```powershell
   .\build_release\release\UpdateServer.exe
   ```

4. **测试**
   客户端将自动升级到 1.5.0

---

## 安全和权限问题

### Q24: 安装程序签名和来源验证

**症状：**
```
Windows SmartScreen 警告："无法识别的发布者"
```

**原因：** 安装程序未使用代码签名证书

**解决方案：**

**短期（开发）：** 使用 Windows SmartScreen 排除

```powershell
# 右键 EXE → 属性 → 安全 → 解除阻止
Unblock-File "AppManagerSetup_1.0.0.exe"
```

**长期（生产）：** 获取代码签名证书

```powershell
# 使用 signtool 进行签名（需要证书）
signtool sign /f certificate.pfx /p password /t timestampUrl `
             AppManagerSetup_1.0.0.exe
```

---

### Q25: 临时文件安全问题

**症状：**
```
%TEMP% 目录中的 EXE 被防病毒软件报警
```

**原因：** 防病毒软件误报或安装程序存在感染迹象

**解决方案：**

1. **禁用 Windows Defender 实时扫描（临时）**
   ```powershell
   Set-MpPreference -DisableRealtimeMonitoring $false  # 扫描后重新启用
   ```

2. **将 AppManager 添加到排除列表**
   ```powershell
   Add-MpPreference -ExclusionPath "C:\Program Files\AppManager"
   ```

3. **扫描并修复**
   ```powershell
   # 完整系统扫描
   Start-MpScan -ScanType FullScan
   ```

---

### Q26: 多用户权限冲突

**症状：**
```
用户 A 成功安装，用户 B 安装失败
或：用户 B 安装后覆盖了用户 A 的配置
```

**原因：** 安装目录权限配置不当

**解决方案：**

编辑 `AppManager/installer/AppManager.iss`，配置每用户安装：

```ini
[Setup]
; 每用户安装而不是系统级
DefaultDirName={userpf}\AppManager
DefaultGroupName={commonappdata}\AppManager

[Registry]
; 写入到 HKEY_CURRENT_USER 而不是 HKEY_LOCAL_MACHINE
Root: HKCU; Subkey: Software\AppManager; ...

[Files]
; 各用户独立配置文件
Source: "apps.json"; DestDir: "{userpf}\AppManager"; ... 
```

---

### Q27: 日志和审计信息不足

**症状：**
```
无法追踪更新失败的原因
```

**原因：** 日志记录不充分

**解决方案：**

编辑 `AppManager/appmanagerservice.cpp`，增加详细日志：

```cpp
void AppManagerService::checkAppManagerUpdate() {
    qDebug() << "[AppManager] 检查更新开始";
    qDebug() << "[AppManager] 当前版本:" << appManagerVersion();
    qDebug() << "[AppManager] 查询 URL:" << updateCheckUrl;
    
    connect(reply, QOverload<QNetworkReply::NetworkError>::of(&QNetworkReply::error),
        [this](QNetworkReply::NetworkError error) {
            qWarning() << "[AppManager] 网络错误:" << error;
            qWarning() << "[AppManager] 错误信息:" << reply->errorString();
        });
    
    // ...
}
```

配置日志输出到文件：
```bash
# 启动时重定向日志
AppManager.exe 2>app.log
```

---

## 总体建议

📌 **最佳实践：**

1. ✅ 使用版本控制管理安装程序和配置
2. ✅ 始终备份旧版本（至少保留最近 3 个版本）
3. ✅ 在生产环境前进行充分测试
4. ✅ 实施日志记录，便于问题诊断
5. ✅ 定期检查磁盘空间和网络性能
6. ✅ 文档化服务端配置和部署流程
7. ✅ 考虑实施灰度发布策略（限制初始用户比例）

📞 **如仍需帮助：**

1. 查看 UpdateServer 的 `server.log` 了解服务端日志
2. 在 AppManager 中通过菜单 → 帮助 → 关于查看版本信息
3. 使用 PowerShell 的 `curl` 命令直接测试 HTTP 接口
4. 检查 Windows 事件查看器的应用程序事件日志

---

**文档版本：** 1.0  
**最后更新：** 2026-03-12  
**适用版本：** AppManager 1.0.0 - 2.x
