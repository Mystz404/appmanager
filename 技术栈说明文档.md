# 升级软件系统技术栈详细说明文档

> 本文档描述 **AppManager（客户端）** 与 **UpdateServer（服务端）** 两个应用的完整技术栈，包括开发环境、框架版本、核心模块设计、通信协议、系统 API 调用及数据格式等。

---

## 一、开发环境与工具链

| 项目 | 说明 |
|------|------|
| **操作系统** | Windows（目标平台为 Windows 桌面） |
| **IDE** | Qt Creator |
| **Qt 版本** | Qt 5.15.2 |
| **编译器** | MinGW 8.1.0 32-bit (i686-w64-mingw32-g++) |
| **C++ 标准** | C++17 (`CONFIG += c++17`，实际编译标志 `gnu++1z`) |
| **构建系统** | qmake (`.pro` 工程文件) |
| **目标架构** | x86 (32-bit Windows) |

---

## 二、Qt 模块依赖

两个项目的 `.pro` 文件均使用以下 Qt 模块：

```qmake
QT += core gui network
greaterThan(QT_MAJOR_VERSION, 4): QT += widgets
```

| Qt 模块 | 用途 |
|---------|------|
| **QtCore** | 基础类型（QString、QVector、QHash、QJsonDocument 等）、事件循环（QEventLoop）、文件 I/O（QFile、QDir）、进程管理（QProcess）、定时器（QTimer）、临时目录（QTemporaryDir） |
| **QtGui** | 图形基础设施、图标提取（QFileIconProvider、QIcon） |
| **QtWidgets** | 全部 UI 控件：QMainWindow、QDialog、QListWidget、QPushButton、QProgressBar、QStackedWidget、QLabel、QPlainTextEdit、QTableWidget、QComboBox、QCheckBox、QSpinBox、QFormLayout、QVBoxLayout、QHBoxLayout、QMessageBox、QFileDialog、QDialogButtonBox 等 |
| **QtNetwork** | 网络通信：QNetworkAccessManager（HTTP 客户端）、QNetworkRequest、QNetworkReply、QTcpServer、QTcpSocket（HTTP 服务器）、QNetworkInterface（获取本机 IP） |

---

## 三、Windows 原生 API

系统通过 P/Invoke 方式直接调用多个 Windows API，无需第三方库。

### 3.1 文件版本信息读取 (`version.dll`)

链接方式：`.pro` 文件中 `win32: LIBS += -lversion`

| API 函数 | 用途 |
|----------|------|
| `GetFileVersionInfoSizeW` | 获取 EXE/DLL 文件版本信息块的大小 |
| `GetFileVersionInfoW` | 读取版本信息块到内存缓冲区 |
| `VerQueryValueW` | 从版本信息块中查询 `VS_FIXEDFILEINFO` 结构体 |

**应用场景：**
- 客户端：`appCurrentVersion()` 从本地 EXE 文件直接读取当前版本号，替代传统的版本配置文件方式
- 服务端：`AppEditDialog::onBrowsePackage()` 选择升级包 EXE 时自动读取版本号

**实现文件**：`versionutils.cpp` — `getFileVersion()` 函数，返回 `"Major.Minor.Build.Revision"` 格式字符串

### 3.2 进程管理 (`kernel32.dll`)

| API 函数 | 用途 |
|----------|------|
| `CreateToolhelp32Snapshot` | 创建系统进程快照 |
| `Process32FirstW` / `Process32NextW` | 遍历进程列表 |
| `OpenProcess` | 以 `PROCESS_TERMINATE \| SYNCHRONIZE` 权限打开目标进程 |
| `TerminateProcess` | 强制终止目标进程 |
| `WaitForSingleObject` | 等待进程完全退出（上限 5 秒） |
| `CloseHandle` | 释放句柄 |

**应用场景：**
- 升级前检测被升级程序是否正在运行 (`isProcessRunning`)
- 用户确认后强制关闭目标程序 (`terminateProcess`)
- 启动应用前检测是否已运行（避免重复启动）

### 3.3 窗口管理 (`user32.dll`)

| API 函数 | 用途 |
|----------|------|
| `EnumWindows` | 枚举所有顶层窗口 |
| `GetWindowThreadProcessId` | 获取窗口所属进程 ID |
| `IsWindowVisible` | 判断窗口是否可见 |
| `IsIconic` | 判断窗口是否最小化 |
| `ShowWindow(SW_RESTORE)` | 还原最小化窗口 |
| `SetForegroundWindow` | 将窗口调至前台 |

**应用场景：** 用户点击应用图标时，若程序已在运行则将其现有窗口调到前台，而非重复启动新实例

**注意事项**：`EnumWindows` 要求回调函数使用 `__stdcall` 调用约定，MinGW 32-bit 下 lambda 默认使用 `__cdecl`，因此回调提取为独立的 `static BOOL CALLBACK` 函数。

---

## 四、第三方依赖

| 依赖项 | 说明 |
|--------|------|
| **无** | 本系统不依赖任何第三方 C/C++ 库。所有功能均基于 Qt 5.15.2 框架和 Windows 原生 API 实现 |

> PowerShell 的 `System.IO.Compression.ZipFile`（.NET Framework 内置类）通过子进程调用方式使用，不构成编译时依赖。

---

## 五、客户端 (AppManager) 架构

### 5.1 项目结构

```
AppManager/
├── AppManager.pro              # qmake 工程文件
├── main.cpp                    # 应用入口
├── mainwindow.h/cpp/ui         # 主窗口（应用启动台）
├── updatedialog.h/cpp          # 升级向导对话框（四阶段流程）
├── appmanagerservice.h/cpp     # 核心业务服务层
├── apptypes.h                  # 数据结构定义
├── versionutils.h/cpp          # 版本比较 & EXE 版本读取工具
├── apps.json                   # 本地应用配置文件
└── zip_replace_manifest.json   # ZIP 升级时文件替换清单
```

### 5.2 核心模块

#### 5.2.1 数据结构 (`apptypes.h`)

| 结构体 | 用途 |
|--------|------|
| `AppConfig` | 本地应用配置：`id`（唯一标识）、`name`（显示名）、`exeRelativePath`（EXE 路径）、`requiredRelativeFiles`（本地必需文件列表）、`updateMetaUrl`（在线元数据 URL） |
| `OnlineAppInfo` | 在线版本信息：`latestVersion`（最新版本）、`downloadUrl`（下载地址）、`sha256`（校验值）、`packageType`（exe/zip）、`installMode`（single/allApps）、`installRelativeDir`、`zipReplaceExeRecursively`、`requiredFiles`（服务端依赖文件列表）、`fullPackageUrl`（完整包下载地址） |
| `AppRuntimeState` | 界面运行状态快照：`currentVersion`（通过 Windows API 从 EXE 文件读取）、`onlineVersion`、`missingFiles`、`hasUpdate`、`statusText` |

#### 5.2.2 业务服务层 (`AppManagerService`)

继承 `QObject`，职责：

1. **配置加载**：解析 `apps.json`，构建应用列表，支持相对/绝对路径的 `appsRoot`
2. **文件完整性检查**：`checkRequiredFiles()` 校验 EXE 及必需文件是否存在
3. **版本获取**：`appCurrentVersion()` 调用 `getFileVersion()` 从 EXE 文件读取版本号
4. **在线检测**：`checkOnlineInfo()` 通过 HTTP GET 请求服务端元数据 JSON，解析版本、下载地址、依赖文件等信息
5. **升级执行**：`upgradeApp()` 支持两种升级模式：
   - **EXE 替换** (`upgradeByExeReplace`)：备份 → 删除旧文件 → 复制新文件，失败时回滚
   - **ZIP 解压** (`upgradeByZipExtract`)：下载 ZIP → PowerShell 解压 → 递归替换 EXE / 覆盖非 EXE 文件 / 补齐缺失文件和目录
6. **依赖修复**：`checkAndFixDependencies()` 检查服务端指定文件列表 → 缺失时下载完整包 → 解压 → 复制 → 再次校验
7. **HTTP 通信**：
   - `httpGet()` 基于 `QNetworkAccessManager` + `QEventLoop` 的同步 HTTP GET
   - `downloadFileWithResume()` 支持 `Range` 请求的断点续传下载

**网络模型**：使用 `QEventLoop` + `QTimer` 实现同步等待模式，避免复杂的异步回调嵌套。`QNetworkAccessManager` 实例全局复用，通过连接池提升多应用检查/下载效率。

**回调模式**：升级/修复方法使用三组 `std::function` 回调：
- `DownloadProgressCallback(qint64 received, qint64 total)` — 下载进度
- `StatusCallback(const QString &status)` — 状态文本
- `InstallProgressCallback(int percent)` — 安装进度百分比

#### 5.2.3 主窗口 (`MainWindow`)

- **布局**：垂直布局 = 标题 + 按钮栏 + 应用图标列表 (QListWidget IconMode) + 日志面板 (QPlainTextEdit)
- **图标显示**：`QFileIconProvider` 从 EXE 文件提取系统图标，图标下方显示应用名、版本号、升级状态
- **日志系统**：日志同时写入文件 (`appmanager.log`) 和界面面板，带时间戳
- **样式**：通过 `qApp->setStyleSheet()` 全局 QSS 设置现代化 UI 风格

#### 5.2.4 升级对话框 (`UpdateDialog`)

四阶段向导式对话框，使用 `QStackedWidget` 管理页面切换：

| 阶段 | 页面 | 功能 |
|------|------|------|
| **检测** (PageCheck) | 进度条 + 状态标签 | 逐个向服务端请求在线元数据，比较版本号 |
| **选择** (PageSelect) | 复选列表 + 全选/全不选 | 用户勾选需要升级的应用 |
| **升级** (PageUpgrade) | 进度条 + 状态标签 | 检测进程 → 关闭进程 → 检查依赖 → 下载 → 校验 → 安装 |
| **结果** (PageResult) | 标题 + 详情文本 | 显示升级成功/失败统计 |

**升级阶段的额外功能：**
- 检测被升级 EXE 是否正在运行，弹框确认后终止程序并记录到 `m_closedApps`
- 升级前调用 `checkAndFixDependencies()` 检查服务端指定的依赖文件
- 全部升级完成后自动重启先前关闭的程序 (`relaunchClosedApps()` via `QProcess::startDetached`)

### 5.3 版本工具 (`versionutils`)

| 函数 | 功能 |
|------|------|
| `compareVersions(left, right)` | 语义化版本比较，按 `.` 分段逐段整数比，不足段补 0。返回 -1/0/1 |
| `getFileVersion(exePath)` | 调用 Windows Version API 读取 EXE 的 `VS_FIXEDFILEINFO`，返回 `"Major.Minor.Build.Revision"` |

此模块被客户端和服务端共同使用（服务端通过相对路径引用源文件）。

---

## 六、服务端 (UpdateServer) 架构

### 6.1 项目结构

```
UpdateServer/
├── UpdateServer.pro            # qmake 工程文件
├── main.cpp                    # 应用入口
├── mainwindow.h/cpp            # 管理界面主窗口
├── httpserver.h/cpp            # 轻量级 HTTP 服务器
├── appupdatemanager.h/cpp      # 应用升级包管理器
├── appeditdialog.h/cpp         # 应用条目编辑对话框
└── (引用) ../AppManager/versionutils.h/cpp  # 共享的版本工具
```

### 6.2 核心模块

#### 6.2.1 HTTP 服务器 (`HttpServer`)

基于 **QTcpServer** 实现的轻量级 HTTP 服务器，**零第三方依赖**，纯 Qt 网络层实现。

**路由表：**

| 路由 | 方法 | 功能 |
|------|------|------|
| `GET /` | GET | 健康检查，返回 `{"status":"ok","server":"UpdateServer"}` |
| `GET /updates/{appId}.json` | GET | 返回指定应用的升级元数据 JSON |
| `GET /download/{fileName}` | GET | 返回 `packages/` 目录下的升级包文件（二进制流） |

**技术特性：**

- **HTTP 协议解析**：手动解析请求行与请求头，提取 Method、Path 和 Range 头
- **路由匹配**：使用 `QRegularExpression` 进行 URL 路径匹配
- **Content-Type 自动识别**：根据文件后缀返回 `application/json`、`application/zip`、`application/x-msdownload` 或 `application/octet-stream`
- **断点续传支持**：解析 `Range: bytes=start-end` 请求头，返回 `206 Partial Content` 和 `Content-Range` 响应头
- **分块传输**：大文件以 64KB 分块写入 socket，避免一次性加载超大文件到内存
- **CORS 支持**：所有响应包含 `Access-Control-Allow-Origin: *`
- **本机 IP 发现**：通过 `QNetworkInterface::allAddresses()` 找到第一个非回环 IPv4 地址，生成 `baseUrl`

**设计定位**：仅适用于内网 / 少量客户端场景，非高并发 Web 服务器。

#### 6.2.2 升级包管理器 (`AppUpdateManager`)

继承 `QObject`，职责：

| 功能 | 说明 |
|------|------|
| **持久化** | 从 `server_apps.json` 加载/保存应用升级元数据（QJsonDocument 读写） |
| **CRUD** | `entries()`、`entry(appId)`、`hasEntry()`、`upsertEntry()`、`removeEntry()` |
| **SHA256 自动计算** | `upsertEntry()` 时自动计算升级包的 SHA256 校验值（`QCryptographicHash::Sha256`，64KB 分块） |
| **元数据生成** | `buildClientMeta()` 根据 baseUrl 拼接 downloadUrl、fullPackageUrl，生成客户端所需的完整 JSON |

**数据模型 (`AppUpdateEntry`)**：

```
appId / appName / latestVersion / packageFileName / sha256 /
packageType (exe|zip) / installMode (single|allApps) /
installRelativeDir / zipReplaceExeRecursively /
requiredFiles[] / fullPackageFileName
```

#### 6.2.3 管理界面 (`MainWindow`)

- **表格视图**：`QTableWidget` 展示所有注册应用的 ID、名称、版本、包文件、SHA256 等信息
- **操作按钮**：新增 / 编辑 / 删除应用条目，刷新列表
- **服务器控制**：启动/停止 HTTP 服务，可配置端口号（`QSpinBox`）
- **实时日志**：`QPlainTextEdit` 显示 HTTP 请求日志（方法、路径、状态码）
- **状态展示**：显示当前监听端口和外部访问 URL

#### 6.2.4 应用编辑对话框 (`AppEditDialog`)

- **表单布局**：`QFormLayout` 管理所有输入字段
- **自动化功能**：
  - 选择 EXE 升级包时自动读取版本号（通过 `getFileVersion()`）
  - 版本号字段只读，避免手动输入错误
  - 自动将非 `packages/` 目录的文件复制到 `packages/` 下
  - 自动计算 SHA256 校验值
- **依赖配置**：多行文本框配置依赖文件路径列表，完整包文件名配置

---

## 七、客户端-服务端通信协议

### 7.1 在线元数据请求

```
客户端 —— GET ——> http://{serverIP}:{port}/updates/{appId}.json
          <—— 200 OK ——
```

**响应 JSON 示例：**

```json
{
    "latestVersion": "1.3.0.0",
    "downloadUrl": "http://192.168.1.100:8080/download/AppA.exe",
    "sha256": "a1b2c3d4...",
    "packageType": "exe",
    "installMode": "single",
    "installRelativeDir": "",
    "zipReplaceExeRecursively": true,
    "requiredFiles": [
        "managed_apps/AppA/config.ini",
        "managed_apps/AppA/data/template.tpl"
    ],
    "fullPackageUrl": "http://192.168.1.100:8080/download/AppA_full.zip"
}
```

### 7.2 升级包下载

```
客户端 —— GET ——> http://{serverIP}:{port}/download/{fileName}
          <—— 200 OK / 206 Partial Content ——
```

- 支持 `Range` 请求实现断点续传
- 大文件分块传输（64KB/块）
- 下载完成后客户端使用 `QCryptographicHash::Sha256` 校验完整性

### 7.3 数据流向

```
┌─────────────────────────────────────────────────────────────────┐
│ 服务端 (UpdateServer)                                           │
│  server_apps.json ──> AppUpdateManager ──> buildClientMeta()    │
│                       ↕                   ↓                     │
│                   packages/           HttpServer                │
│                   ├── AppA.exe        GET /updates/{id}.json    │
│                   ├── AppA.zip        GET /download/{file}      │
│                   └── AppA_full.zip                              │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTP (内网)
┌──────────────────────────────▼──────────────────────────────────┐
│ 客户端 (AppManager)                                             │
│  apps.json ──> AppManagerService                                │
│                ├── checkOnlineInfo()  ←── GET /updates/         │
│                ├── upgradeApp()       ←── GET /download/        │
│                ├── checkAndFixDependencies() ←── GET /download/ │
│                └── appCurrentVersion() ←── Windows Version API  │
│                                                                  │
│  MainWindow (应用启动台)                                         │
│  └── UpdateDialog (四阶段升级向导)                               │
│      ├── runCheckPhase()    检测                                 │
│      ├── showSelectPhase()  选择                                 │
│      ├── runUpgradePhase()  升级（含进程管理/依赖修复）           │
│      └── showResultPhase()  结果                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 八、数据存储与配置文件

### 8.1 客户端配置 (`apps.json`)

```json
{
    "appsRoot": ".",
    "apps": [
        {
            "id": "bctester",
            "name": "BCMU上位机",
            "exe": "managed_apps/BCTester.exe",
            "requiredFiles": [
                "managed_apps/bctester.ini"
            ],
            "updateMetaUrl": "http://127.0.0.1:8080/updates/bctester.json"
        }
    ]
}
```

| 字段 | 说明 |
|------|------|
| `appsRoot` | 应用根目录（相对/绝对），所有应用路径基于此解析 |
| `apps[].id` | 应用唯一标识，建议字母数字下划线 |
| `apps[].name` | 显示名称 |
| `apps[].exe` | 主程序 EXE 相对路径 |
| `apps[].requiredFiles` | 本地必需文件列表（启动前检查） |
| `apps[].updateMetaUrl` | 在线升级元数据 URL |

### 8.2 服务端配置 (`server_apps.json`)

```json
{
    "packagesDir": "/path/to/packages",
    "apps": [
        {
            "appId": "bctester",
            "appName": "BCMU上位机",
            "latestVersion": "1.3.0.0",
            "packageFileName": "BCTester.exe",
            "sha256": "a1b2c3...",
            "packageType": "exe",
            "installMode": "single",
            "installRelativeDir": "",
            "zipReplaceExeRecursively": true,
            "requiredFiles": ["managed_apps/bctester.ini"],
            "fullPackageFileName": "BCTester_full.zip"
        }
    ]
}
```

### 8.3 客户端日志 (`appmanager.log`)

追加写入模式，每行格式：`[yyyy-MM-dd HH:mm:ss] 消息内容`

---

## 九、升级流程详解

### 9.1 EXE 替换升级

```
下载 EXE → SHA256 校验 → 备份旧 EXE (.bak) → 删除旧 EXE → 复制新 EXE → 完成
                                                   ↓ (失败时)
                                              从 .bak 回滚
```

### 9.2 ZIP 解压升级

```
下载 ZIP → SHA256 校验
         → PowerShell 解压到临时目录
         → 根据 installMode 确定目标:
           ├── single: exe 所在目录 或 installRelativeDir
           └── allApps: appsRoot（全局替换）
         → zipReplaceExeRecursively=true 时:
           ├── 递归替换所有 EXE（带备份和回滚）
           └── 递归复制所有非 EXE 文件
         → zipReplaceExeRecursively=false 时:
           └── 仅补齐缺失文件和目录
```

**ZIP 解压实现**：通过 `QProcess` 调用 PowerShell，使用 .NET `System.IO.Compression.ZipFile` 类解压，支持逐个文件的进度报告 (`PROGRESS:{percent}`)。

### 9.3 依赖修复流程

```
服务端提供 requiredFiles[] 列表
→ 客户端逐项检查本地文件是否存在
→ 全部存在: 跳过
→ 有缺失:
  → 服务端提供 fullPackageUrl:
    → 下载完整包 ZIP
    → 解压到临时目录
    → 递归复制到目标目录
    → 再次校验依赖完整性
  → 服务端未配置完整包:
    → 返回失败，提示用户
```

### 9.4 进程管理流程

```
升级前:
→ CreateToolhelp32Snapshot 遍历进程列表
→ 匹配 EXE 文件名（不区分大小写）
→ 发现运行中: QMessageBox 提示用户确认
  → 确认: TerminateProcess + WaitForSingleObject(5s) → 记录到 m_closedApps
  → 拒绝: 跳过该应用升级

升级后:
→ 遍历 m_closedApps
→ QProcess::startDetached 重新启动每个程序
```

---

## 十、安全机制

| 机制 | 实现方式 |
|------|----------|
| **升级包完整性校验** | SHA256 校验（`QCryptographicHash`），服务端自动计算并发布，客户端下载后比对 |
| **EXE 备份与回滚** | 替换前创建 `.bak` 备份，替换失败时自动恢复 |
| **文件权限处理** | 删除前设置 `WriteUser` 权限位 (`QFileDevice::setPermissions`) |
| **超时控制** | HTTP 请求均有超时限制（默认 10s 检测 / 30s 升级 / 180s 依赖修复） |
| **进程安全终止** | `TerminateProcess` 后调用 `WaitForSingleObject` 等待退出 |

---

## 十一、UI 样式方案

客户端采用 **全局 QSS** 方案（`qApp->setStyleSheet()`），主要设计要素：

| 元素 | 样式 |
|------|------|
| 背景 | `#f5f7fb` 浅灰蓝 |
| 按钮 | `#2563eb` 蓝色系，rounded 8px，hover/pressed 渐深 |
| 图标列表 | 白色背景，圆角 `10px`，选中态 `#dbeafe` 浅蓝 |
| 日志面板 | 暗色终端风格 (`#1e293b` 深灰 + `#a3e635` 荧光绿)、等宽字体 (Consolas) |

服务端界面为标准 Qt Widgets 布局，未做额外样式定制。

---

## 十二、构建与部署

### 12.1 构建命令

```powershell
# 设置 MinGW 到 PATH
$env:PATH = "C:\Qt\Tools\mingw810_32\bin;" + $env:PATH

# 客户端
cd "d:\WorkSpace\QtProjects\AppManager\build\Desktop_Qt_5_15_2_MinGW_32_bit-Release"
C:\Qt\5.15.2\mingw81_32\bin\qmake.exe ..\..\AppManager.pro -spec win32-g++ "CONFIG+=qtquickcompiler"
mingw32-make -j4

# 服务端
cd "d:\WorkSpace\QtProjects\UpdateServer\build"
C:\Qt\5.15.2\mingw81_32\bin\qmake.exe ..\UpdateServer.pro -spec win32-g++ "CONFIG+=qtquickcompiler"
mingw32-make -j4
```

### 12.2 部署要点

- 客户端 `AppManager.exe` 与 `apps.json` 放在同一目录
- 被管理的应用 EXE 放在 `apps.json` 中配置的相对路径位置
- 服务端 `UpdateServer.exe` 与 `server_apps.json` 放在同一目录
- 升级包文件放入服务端的 `packages/` 目录
- 客户端 `updateMetaUrl` 指向服务端对应的 `/updates/{appId}.json` 地址

---

## 十三、代码量统计

| 模块 | 文件数 | 关键文件概况 |
|------|--------|-------------|
| **客户端** | 9 个源文件 (5 .cpp + 4 .h) + 1 .ui + 2 .json | appmanagerservice.cpp ~1545行，updatedialog.cpp ~631行，mainwindow.cpp ~361行 |
| **服务端** | 9 个源文件 (5 .cpp + 4 .h) | httpserver.cpp ~336行，appupdatemanager.cpp ~272行，appeditdialog.cpp ~260行 |
| **共享** | versionutils.h/cpp | ~67行 |

---

*文档生成日期：2026-03-04*
