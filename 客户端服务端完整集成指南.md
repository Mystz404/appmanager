# AppManager 自更新 - 完整集成指南

## 🎯 目标

本指南说明如何完整构建 AppManager 自更新功能的完整链路：
- 客户端：AppManager.exe（检查版本、下载、安装）
- 服务端：UpdateServer（管理升级包、提供元数据）

## 📋 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     AppManager 客户端                        │
│                                                              │
│  用户菜单 → 帮助 → 检查AppManager更新                        │
│                 ↓                                            │
│          onCheckAppManagerUpdate()                           │
│                 ↓                                            │
│      checkAppManagerUpdate() [Service]                       │
│                 ↓                                            │
│      GET /updates/AppManager.json                           │
└─────────────────────────────────────────────────────────────┘
                           ↓↑
                        网络请求
                           ↓↑
┌─────────────────────────────────────────────────────────────┐
│                    UpdateServer 服务端                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ server_apps.json                                     │   │
│  │ {                                                    │   │
│  │   "appId": "AppManager",                             │   │
│  │   "latestVersion": "2.0.0",                          │   │
│  │   "packageFileName": "AppManagerSetup_2.0.0.exe"    │   │
│  │ }                                                    │   │
│  └──────────────────────────────────────────────────────┘   │
│               ↓                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ AppUpdateManager                                     │   │
│  │ → 加载 server_apps.json                              │   │
│  │ → 计算包文件 SHA256                                  │   │
│  │ → 生成 updates/AppManager.json                       │   │
│  └──────────────────────────────────────────────────────┘   │
│               ↓                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ HttpServer                                           │   │
│  │ ├─ GET /updates/AppManager.json                      │   │
│  │ ├─ GET /download/AppManagerSetup_2.0.0.exe         │   │
│  │ └─ (其他应用的接口)                                 │   │
│  └──────────────────────────────────────────────────────┘   │
│               ↓                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ packages/AppManager/                                 │   │
│  │ ├─ AppManagerSetup_1.0.0.exe                        │   │
│  │ ├─ AppManagerSetup_1.0.1.exe                        │   │
│  │ └─ AppManagerSetup_2.0.0.exe                        │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  updates/AppManager.json (自动生成)                          │
│  {                                                           │
│    "latestVersion": "2.0.0",                                │
│    "downloadUrl": "http://ip:port/download/...",           │
│    "sha256": "abc123...",                                   │
│    "packageType": "exe"                                     │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
                           ↓↑
                        网络下载
                           ↓↑
┌─────────────────────────────────────────────────────────────┐
│                     AppManager 客户端                        │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 下载进度对话框                                       │   │
│  │ GET /download/AppManagerSetup_2.0.0.exe             │   │
│  │ → %TEMP%/AppManagerSetup_2.0.0.exe                  │   │
│  └──────────────────────────────────────────────────────┘   │
│               ↓                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 启动安装程序                                         │   │
│  │ QProcess::startDetached(installer, "/SILENT")       │   │
│  │ → Inno Setup 安装程序接管                            │   │
│  │ → 关闭当前 AppManager 应用                           │   │
│  │ → 安装完成后重启应用                                │   │
│  └──────────────────────────────────────────────────────┘   │
│               ↓                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 新版本 AppManager (2.0.0)                            │   │
│  │ appManagerVersion() → "2.0.0"                        │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 快速部署清单

### 第 1 步：编译 AppManager（客户端）

```bash
# 编译主程序
cd d:\WorkSpace\QtProjects\AppManager\build\Desktop_Qt_5_15_2_MinGW_32_bit-Release
mingw32-make -j4

# 生成安装程序
cd ..\installer
.\build_installer.ps1 -Version 1.0.0
# 输出：AppManager\installer_output\AppManagerSetup.exe
```

### 第 2 步：配置 UpdateServer（服务端）

1. **编辑 server_apps.json**
   ```bash
   # 位置：UpdateServer/server_apps.json
   # 添加 AppManager 条目（详见示例文件）
   ```

2. **准备升级包**
   ```bash
   # 创建目录
   mkdir UpdateServer\packages\AppManager
   
   # 复制安装程序
   copy AppManager\installer_output\AppManagerSetup.exe `
        UpdateServer\packages\AppManager\AppManagerSetup_1.0.0.exe
   ```

3. **启动服务端**
   ```bash
   # GUI 模式（推荐）
   UpdateServer\build_release\release\UpdateServer.exe
   
   # 或无头模式
   UpdateServer\build_release\release\UpdateServer.exe --headless --port 6655
   ```

4. **验证元数据生成**
   ```bash
   # 检查是否生成了以下文件
   UpdateServer\updates\AppManager.json
   
   # 文件内容示例：
   # {
   #   "latestVersion": "1.0.0",
   #   "downloadUrl": "http://your-ip:6655/download/AppManagerSetup_1.0.0.exe",
   #   "packageType": "exe",
   #   "sha256": "..."
   # }
   ```

### 第 3 步：配置客户端

编辑 `AppManager\apps.json`：

```json
{
    "serverBaseUrl": "http://your-server-ip:6655",
    "appsRoot": ".",
    "apps": [...]
}
```

**关键字段**：
- `serverBaseUrl`：指向服务端的基础 URL

### 第 4 步：测试自更新

1. **启动 AppManager 客户端**
   ```bash
   AppManager\build\Desktop_Qt_5_15_2_MinGW_32_bit-Release\release\AppManager.exe
   ```

2. **检查更新**
   ```
   菜单 → 帮助 → 检查 AppManager 更新
   ```

3. **验证流程**
   - ✅ 显示进度对话框："正在检查 AppManager 新版本..."
   - ✅ 成功获取版本信息
   - ✅ 比较版本号
   - ✅ 显示升级提示（如有新版本）
   - ✅ 下载安装程序（显示进度）
   - ✅ 启动安装程序
   - ✅ 应用自动关闭，安装程序安装
   - ✅ 应用重启成功

## 🔧 常见配置场景

### 场景 1：本地测试（单机）

```
1. 启动 UpdateServer 在本机
   UpdateServer.exe --port 6655
   
2. 配置客户端 apps.json
   "serverBaseUrl": "http://127.0.0.1:6655"
   
3. 测试自更新流程
   AppManager.exe → 菜单 → 检查更新
```

### 场景 2：局域网部署（多机）

```
服务端机器：192.168.1.100

1. 启动 UpdateServer
   \\192.168.1.100\UpdateServer\UpdateServer.exe --headless --port 6655
   
2. 客户端配置
   "serverBaseUrl": "http://192.168.1.100:6655"
   
3. 所有客户端可通过网络检查和安装更新
```

### 场景 3：云端部署（互联网）

```
云服务器：cloud.example.com

1. 部署 UpdateServer 到云主机
   确保 6655 端口可公网访问
   
2. 客户端配置
   "serverBaseUrl": "https://cloud.example.com:6655"
   
3. 全球客户端可自动更新
```

## 📊 版本管理

### 更新到新版本的流程

假设当前版本是 1.0.0，要更新到 2.0.0：

1. **编译新版本**
   ```bash
   # AppManager 源代码更新到 2.0.0
   cd AppManager
   git pull origin main
   git checkout v2.0.0  # 或具体 commit
   
   # 编译
   qmake && mingw32-make -j4
   ```

2. **生成新安装程序**
   ```bash
   cd installer
   .\build_installer.ps1 -Version 2.0.0
   # 输出：AppManagerSetup_2.0.0.exe
   ```

3. **部署到服务端**
   ```bash
   copy installer_output\AppManagerSetup.exe `
        UpdateServer\packages\AppManager\AppManagerSetup_2.0.0.exe
   ```

4. **更新 server_apps.json**
   ```json
   {
       "appId": "AppManager",
       "latestVersion": "2.0.0",
       "packageFileName": "AppManagerSetup_2.0.0.exe",
       ...
   }
   ```

5. **重启服务端**（或点击"刷新"）
   ```bash
   # 服务端自动：
   # 1. 读取 packages/AppManager/AppManagerSetup_2.0.0.exe
   # 2. 计算 SHA256
   # 3. 生成 updates/AppManager.json
   ```

6. **验证**
   ```bash
   # 检查生成的元数据
   cat UpdateServer\updates\AppManager.json
   # 应该显示 "latestVersion": "2.0.0"
   ```

### 版本共存

可以在 `packages/AppManager/` 中同时存放多个版本：

```
packages/AppManager/
├── AppManagerSetup_1.0.0.exe  ← 旧版本（备用）
├── AppManagerSetup_1.0.1.exe  ← 中间版本
└── AppManagerSetup_2.0.0.exe  ← 当前最新版本
```

但 `server_apps.json` 中的 `latestVersion` 只能指向一个版本。

## 🔒 安全相关

### URL 重定基（多服务器场景）

如果服务端返回的 URL 包含错误的 IP/域名，客户端会自动重定基：

```
服务端返回：
  downloadUrl: "http://localhost:8080/download/..."
  
客户端自动调整为：
  downloadUrl: "http://192.168.1.100:6655/download/..."
  
（基于 apps.json 中的 serverBaseUrl）
```

### SHA256 验证

建议为安装程序添加 SHA256 校验：

```bash
# 生成 SHA256
(Get-FileHash "AppManagerSetup_2.0.0.exe" -Algorithm SHA256).Hash

# 配置到 server_apps.json
"sha256": "abc123def456..."
```

### HTTPS 支持

如果需要 HTTPS：

```json
{
    "serverBaseUrl": "https://your-server-ip:6655"
}
```

需要服务端配置 SSL 证书（当前 UpdateServer 未实现，可通过反向代理如 nginx 实现）。

## 🐛 故障排查

### 检查失败排查表

| 症状 | 原因 | 解决方法 |
|------|------|---------|
| "检查更新失败" | 网络不通 | `ping` 服务端 IP，检查端口 6655 |
| "检查更新失败" | URL 无效 | 验证 `serverBaseUrl` 配置，确保可访问 |
| "检查更新失败" | 文件缺失 | 检查 `updates/AppManager.json` 是否存在 |
| 下载失败 | 文件不存在 | 检查 `packages/AppManager/` 目录中的文件 |
| 下载失败 | 网络中断 | 让客户端重试（支持多次重试） |
| 安装失败 | 权限不足 | 以管理员身份运行 AppManager |
| 安装失败 | 文件被占用 | 关闭其他 AppManager 进程后重试 |

### 调试命令

**验证服务端元数据**
```bash
curl http://your-ip:6655/updates/AppManager.json
```

**检查下载接口**
```bash
curl -I http://your-ip:6655/download/AppManagerSetup_2.0.0.exe
# 应返回 200 OK，显示文件大小
```

**查看服务端日志**
```bash
# GUI 模式：在 MainWindow 中查看日志窗口
# 无头模式：查看 server.log 文件
tail -f UpdateServer/server.log
```

## 📈 性能优化

### 下载优化

- 支持断点续传：客户端异常中断可继续下载
- 分块传输：64KB 块大小，避免内存溢出
- 异步 I/O：服务端非阻塞文件传输

### 版本检查优化

- 缓存元数据：客户端可缓存最近检查结果
- 条件请求：可使用 HTTP 304 Not Modified 减少下载
- 批量检查：对多个应用进行检查（通过建立会话）

## 🎓 扩展功能

### 灰度发布（增强功能）

修改 UpdateServer 的 HttpServer，支持查询参数：

```
GET /updates/AppManager.json?clientId={uuid}&version={current}
```

服务端可根据客户端信息返回不同版本。

### 强制更新

在 `server_apps.json` 中添加字段（需修改客户端）：

```json
{
    "appId": "AppManager",
    "forceUpdate": true,
    "minSupportedVersion": "1.0.0"
}
```

客户端逻辑：版本 < minSupportedVersion 时强制升级。

### 更新日志

服务端返回更新说明：

```json
{
    "latestVersion": "2.0.0",
    "releaseNotes": "修复 xxx 问题\n新增 yyy 功能",
    "downloadUrl": "..."
}
```

客户端显示在升级提示中。

## 📞 关键文件清单

### 客户端文件

| 文件 | 位置 | 用途 |
|------|------|------|
| appmanagerservice.h/cpp | AppManager/ | 自更新核心逻辑 |
| mainwindow.h/cpp | AppManager/ | UI 菜单和交互 |
| apps.json | <app> | 配置文件（包含 serverBaseUrl） |
| AppManager.exe | build/.../release/ | 主程序 |
| AppManagerSetup_*.exe | installer_output/ | 装程序 |

### 服务端文件

| 文件 | 位置 | 用途 |
|------|------|------|
| appupdatemanager.h/cpp | UpdateServer/ | 元数据管理 |
| httpserver.h/cpp | UpdateServer/ | HTTP 接口 |
| server_apps.json | UpdateServer/ | 配置文件 |
| packages/AppManager/ | UpdateServer/ | 升级包存储 |
| updates/AppManager.json | UpdateServer/ | 元数据输出 |

## ✅ 部署检查清单

```
客户端部分
  ☐ AppManager 编译成功（Release 版本）
  ☐ 生成了 AppManagerSetup_1.0.0.exe
  ☐ apps.json 中配置了正确的 serverBaseUrl
  ☐ 菜单中显示"检查 AppManager 更新"选项

服务端部分
  ☐ UpdateServer 编译成功
  ☐ server_apps.json 中添加了 AppManager 条目
  ☐ AppManagerSetup_1.0.0.exe 放置到 packages/AppManager/
  ☐ 服务端启动后生成了 updates/AppManager.json
  ☐ updates/AppManager.json 内容有效（latestVersion, downloadUrl）

网络部分
  ☐ 客户端和服务端网络连通
  ☐ serverBaseUrl 在客户端可访问
  ☐ 防火墙允许 HTTP 访问（如需 HTTPS，配置证书）

功能验证
  ☐ 客户端能连接到服务端
  ☐ 检查更新对话框正常显示
  ☐ 版本比较逻辑正确
  ☐ 下载进度显示正常
  ☐ 安装程序启动成功
  ☐ 应用自动关闭并重启
```

## 🎉 总结

AppManager 的自更新功能已经完整实现，包括：

✅ **客户端**：自动检查版本、下载、运行安装程序  
✅ **服务端**：管理升级包、生成元数据、提供 HTTP 接口  
✅ **集成**：通过标准 JSON 和 HTTP 协议连接  
✅ **文档**：完整的部署和故障排查指南  

按照本指南进行配置，可以实现完整的企业级安装程序自更新。
