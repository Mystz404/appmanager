# 客户端与服务端技术栈说明

本文档基于当前代码与工程配置整理，覆盖客户端 AppManager 与服务端 UpdateServer 的实际技术栈。

## 1. 系统组成

整套系统由两个 Windows 桌面程序组成：

| 组件 | 项目 | 角色 |
| --- | --- | --- |
| 客户端 | AppManager | 应用启动、在线更新、文档浏览、登录鉴权 |
| 服务端 | UpdateServer | 升级包管理、文档管理、用户管理、HTTP 分发 |

两端通过 HTTP/JSON 通信，客户端主动拉取应用目录、文档目录、更新元数据和安装包文件。

## 2. 共享基础技术栈

### 2.1 开发语言与平台

- 开发语言：C++
- 语言标准：C++17
- 目标平台：Windows 桌面
- 运行形态：原生桌面 GUI 程序

### 2.2 Qt 框架

- Qt 主版本：Qt 5
- 工程启用模块：Qt Core、Qt Gui、Qt Widgets、Qt Network
- UI 技术：Qt Widgets 手写界面 + 局部 Qt Designer .ui 文件
- 资源管理：Qt Resource System（.qrc）

### 2.3 构建与编译

- 构建系统：qmake（.pro）
- 编译器链：MinGW / GCC
- 编译目标：32 位 Windows 可执行程序
- 构建命令：mingw32-make -f Makefile.Release

### 2.4 数据与协议

- 通信协议：HTTP
- 数据格式：JSON
- 二进制分发：EXE、ZIP、PDF、Office 文档、图片等文件流
- 本地持久化：JSON 文件 + 本地目录结构

### 2.5 Windows 相关能力

- 文件版本读取：version.dll / Win32 版本资源 API
- 进程与窗口控制：Win32 API
- 图标与资源：Windows 图标资源 + Qt 图标资源

## 3. 客户端 AppManager 技术栈

### 3.1 客户端定位

AppManager 是一个基于 Qt Widgets 的 Windows 应用管理客户端，负责：

- 本地应用清单管理
- 应用启动与运行状态处理
- 在线更新检查与升级执行
- 历史版本下载
- 文档目录浏览、下载、打开
- 用户登录与权限感知展示
- AppManager 自身安装包升级

### 3.2 客户端核心框架

- 应用入口：QApplication
- 主窗口：QMainWindow
- 页面切换：QStackedWidget
- 应用列表/文档列表：QListWidget + 自定义 QStyledItemDelegate
- 对话框：QDialog、QMessageBox、QProgressDialog
- 设置存储：QSettings（登录 token、用户名）

### 3.3 客户端 UI 技术

- Qt Widgets 手工构建大部分界面
- mainwindow.ui 仅承担基础主窗体骨架
- 样式实现：Qt StyleSheet
- 自定义卡片渲染：QPainter、QPainterPath、QStyledItemDelegate
- 动画反馈：QMovie
- 多页内容组织：启动台、社区占位、学习文档、日志页

### 3.4 客户端网络栈

- HTTP 客户端：QNetworkAccessManager
- 请求对象：QNetworkRequest
- 响应对象：QNetworkReply
- 同步等待方式：QEventLoop + QTimer 超时控制
- 重定向策略：NoLessSafeRedirectPolicy
- 鉴权头：Authorization: Bearer token
- 断点续传：HTTP Range 请求头

### 3.5 客户端业务技术点

#### 应用管理

- 应用配置模型：AppConfig、OnlineAppInfo、AppRuntimeState
- 本地配置文件：apps.json、client_apps.json、versions.json
- 依赖清单：requiredFiles
- 启动程序：QProcess::startDetached
- 本地文件检测：QFile、QFileInfo、QDir、QDirIterator

#### 在线升级

- 版本比较：自定义 compareVersions
- 文件版本读取：GetFileVersionInfoSizeW / GetFileVersionInfoW / VerQueryValueW
- 包校验：QCryptographicHash::Sha256
- EXE 替换升级：复制、备份、回滚策略
- ZIP 升级：QProcess 调用 PowerShell 解压
- 完整包依赖修复：下载 fullPackageUrl 后补齐目录与文件

#### 文档功能

- 文档模型：ClientDocEntry
- 文档目录接口：GET /docs/catalog
- 文档下载接口：GET /docs/download/{docId}/{fileName}
- 本地文档缓存：客户端 exe 同级 docs 目录
- 文档打开：QDesktopServices / 系统默认程序 / 打开文件位置

#### 登录与权限

- 登录对话框：LoginDialog
- 登录接口：POST /login
- 登录冷却策略：失败次数计数 + QTimer 倒计时
- 登录状态持久化：QSettings
- 权限可见性：客户端通过 token 拉取应用目录和文档目录

#### 自身升级

- 自更新元数据：GET /updates/AppManager.json
- 下载 AppManager 安装程序后通过 QProcess::startDetached 启动安装包

### 3.6 客户端使用的关键 Qt / 系统类

- Qt Core：QObject、QString、QStringList、QHash、QQueue、QSet、QTimer、QDateTime
- Qt Gui：QIcon、QPainter、QPainterPath、QMovie
- Qt Widgets：QMainWindow、QWidget、QListWidget、QLabel、QLineEdit、QPushButton、QToolButton、QStackedWidget、QProgressBar、QPlainTextEdit、QMenu、QDialog
- Qt Network：QNetworkAccessManager、QNetworkRequest、QNetworkReply
- Windows API：CreateToolhelp32Snapshot、Process32FirstW、Process32NextW、EnumWindows、SetForegroundWindow、ShowWindow、GetWindowThreadProcessId

### 3.7 客户端本地数据与目录

- apps.json：客户端基础配置
- client_apps.json：客户端持久化应用列表
- versions.json：版本记录
- zip_replace_manifest.json：ZIP 替换清单
- docs/：文档缓存目录
- appmanager.log：客户端日志

### 3.8 客户端交付链路

- 依赖部署：windeployqt
- 安装包制作：Inno Setup
- 安装脚本：.iss
- 打包自动化：PowerShell 脚本 installer.ps1

## 4. 服务端 UpdateServer 技术栈

### 4.1 服务端定位

UpdateServer 是一个基于 Qt Widgets 的管理端 + 内置 HTTP 文件分发服务，负责：

- 管理应用升级包和版本元数据
- 管理文档目录和文档文件
- 管理用户、登录、应用可见权限
- 向客户端提供应用目录、历史版本、文档目录、下载接口
- 支持 GUI 模式与无头模式

### 4.2 服务端核心框架

- 应用入口：QApplication
- 管理界面：QMainWindow
- 系统托盘：QSystemTrayIcon
- 单实例控制：QLocalSocket + QLocalServer
- GUI/无头一体化：同一进程根据命令行参数切换模式

### 4.3 服务端 HTTP 栈

- TCP 监听：QTcpServer
- 连接处理：QTcpSocket
- 请求分发：手写 HTTP 路由解析
- 文件传输：异步分块发送
- 分块大小：64 KB
- 长连接策略：显式 Connection: close
- 跨域头：Access-Control-Allow-Origin、Access-Control-Allow-Headers、Access-Control-Allow-Methods
- 断点续传：支持 Range / Content-Range

### 4.4 服务端路由能力

- POST /login：用户名密码登录
- GET /：健康检查
- GET /status：运行状态
- GET /catalog：应用目录
- GET /history/{appId}：历史版本目录
- GET /download/{appId}/{fileName}：当前版本安装包下载
- GET /download/history/{appId}/{fileName}：历史版本包下载
- GET /updates/{appId}.json：静态更新元数据
- GET /docs/catalog：文档目录
- GET /docs/download/{docId}/{fileName}：文档下载

### 4.5 服务端业务模块

#### 应用升级管理

- 核心类：AppUpdateManager
- 持久化文件：server_apps.json
- 安装包目录：packages/
- 静态元数据目录：updates/
- 元数据内容：latestVersion、downloadUrl、sha256、packageType、subDir、allowMultiInstance、requiredFiles、history、changeLog
- 静态元数据生成：服务端本地生成 updates/{appId}.json

#### 文档管理

- 核心类：DocManager
- 文档模型：DocEntry
- 持久化文件：server_docs.json
- 文档目录：docs/
- 文档元数据：docId、title、version、categories、description、fileName、sha256、keywords、sortOrder、requiresLogin

#### 用户与权限管理

- 核心类：UserManager
- 持久化文件：server_users.json
- 用户模型：id、username、password、role、allowedApps
- 登录成功后令牌：QUuid 生成
- 令牌存储：内存 QMap
- 权限模型：
  - requiresLogin 控制应用/文档是否仅登录后可见
  - allowedApps 控制某用户能看到哪些应用

注意：当前代码实际使用明文密码比对与持久化，虽然头文件注释写有 SHA-256 哈希，但实现以 usermanager.cpp 为准。

### 4.6 服务端安全与可靠性技术

- 路径穿越防护：QDir::cleanPath + 文件名规则校验
- 请求大小限制：MAX_REQUEST_SIZE
- 连接上限：最大活动连接数限制
- 空闲超时：QTimer 断开超时连接
- 日志系统：Logger 单例 + 文件轮转
- 文件传输：异步写 socket，避免阻塞主线程

### 4.7 服务端界面与运维能力

- Qt Widgets 管理面板
- 应用管理、文档管理、用户管理三类页面
- 请求日志查看：QPlainTextEdit
- 状态展示：监听端口、运行状态、基础 URL
- 无头模式：命令行参数 --headless / -H
- 端口控制：--port / -p
- 托盘恢复与退出：QSystemTrayIcon + 托盘菜单

### 4.8 服务端使用的关键 Qt / 系统类

- Qt Core：QObject、QHash、QMap、QSet、QTimer、QElapsedTimer、QUuid、QRegularExpression
- Qt Network：QTcpServer、QTcpSocket、QNetworkInterface
- Qt Widgets：QMainWindow、QTableWidget、QPushButton、QSpinBox、QLabel、QPlainTextEdit、QTabWidget、QSplitter
- 进程单实例：QLocalSocket、QLocalServer
- 托盘：QSystemTrayIcon

### 4.9 服务端本地数据与目录

- server_apps.json：应用升级配置
- server_docs.json：文档配置
- server_users.json：用户配置
- packages/：应用安装包目录
- updates/：生成后的静态更新元数据目录
- docs/：文档存储目录
- server.log：服务端日志

## 5. 构建、发布与外围支撑技术

### 5.1 构建发布工具

- qmake：生成 Makefile
- mingw32-make：编译 Release 产物
- windeployqt：收集 Qt 运行时依赖
- Inno Setup：生成 AppManager 安装程序
- PowerShell：自动化构建、安装包打包、ZIP 解压辅助

### 5.2 可选部署支撑

- Nginx：反向代理 UpdateServer，对外暴露域名与超时、缓冲、日志策略
- 域名转发/公网接入：通过 publicBaseUrl 与反向代理组合实现

## 6. 技术栈清单汇总

### 客户端 AppManager

- C++17
- Qt Core / Gui / Widgets / Network
- Qt Widgets UI + QSS
- QNetworkAccessManager HTTP 客户端
- JSON 配置与元数据解析
- SHA-256 文件校验
- PowerShell 辅助 ZIP 解压
- Inno Setup 安装包交付
- Win32 API 进程与窗口控制

### 服务端 UpdateServer

- C++17
- Qt Core / Gui / Widgets / Network
- Qt Widgets 管理后台
- QTcpServer / QTcpSocket 自研轻量 HTTP 服务
- JSON 持久化配置与静态元数据生成
- QUuid token 登录鉴权
- QSystemTrayIcon 托盘运维
- QLocalSocket / QLocalServer 单实例唤起
- 异步大文件分块传输 + Range 续传支持

### 共享底层能力

- qmake + mingw32-make
- Windows 原生桌面运行环境
- Qt 资源系统
- JSON 文件作为核心配置与交换格式
- HTTP 作为客户端与服务端通信协议

## 7. 一句话描述技术栈

这套系统本质上是一套基于 C++17 + Qt Widgets + Qt Network 构建、以 JSON 和 HTTP 为通信核心、运行在 Windows 上的原生桌面应用管理与更新分发平台。